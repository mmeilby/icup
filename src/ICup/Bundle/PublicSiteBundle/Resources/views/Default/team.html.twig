{% extends "base.html.twig" %}

{% block body %}
<a href="{{ path('_showcategory', { 'categoryid': category.id }) }}">
    <h1 class="category">{% trans %}CATEGORY{% endtrans %} {{ category.name }}</h1>
</a>
<p class="subcategory">
    {{ [category.gender,category.classification]|join|trans }}
    {{ category.classification|trans }}
</p>
<a href="{{ path('_showcategory', { 'categoryid': category.id }) }}">
    <h2 class="group">{% trans %}GROUP{% endtrans %} {{ group.name }}</h2>
</a>
<p class="subgroup">{{ team.name }}</p>
<p>
    <table>
        <thead>
            <tr>
                <td class="flag"></td>
                <td class="no">{% trans %}LEGEND.NO{% endtrans %}</td>
                <td class="time">{% trans %}LEGEND.TIME{% endtrans %}</td>
                <td class="no">{% trans %}FIELD{% endtrans %}</td>
                <td class="flag"></td>
                <td class="team">{% trans %}TEAM{% endtrans %} A</td>
                <td class="flag"></td>
                <td class="team">{% trans %}TEAM{% endtrans %} B</td>
                <td class="match">{% trans %}LEGEND.RESULT{% endtrans %}</td>
            </tr>
        </thead>
        <tboby>
{% set catdate = '' %}
{% for match in matchlist %}
    {% if match.schedule|date('d-M-Y') != catdate %}
            <tr class="{{ cycle(['even','odd'],loop.index) }}">
                <td class="date_solo" colspan="9">
                    {{ ['WEEK.',match.schedule|date('D')|upper]|join|trans|capitalize }}{{ match.schedule|date(' j. ') }}{{ ['MONTH.',match.schedule|date('M')|upper]|join|trans }}
                </td>
            </tr>
        {% set catdate = match.schedule|date('d-M-Y') %}
    {% endif %}
            <tr class="{{ cycle(['even','odd'],loop.index) }}">
                <td class="flag"></td>
                <td class="no">{{ match.matchno }}</td>
                <td class="time">{{ match.schedule|date('H.i') }}</td>
                <td class="no"><a href="{{ path('_showplayground', { 'playgroundid': match.playgroundid, 'groupid': group.id }) }}" title="{{ match.playground }}">{{ match.playgroundno }}</a></td>
                <td class="flag"><img src="{{ imagepath }}/flags/{{ match.flagA }}" alt="{{ match.countryA|trans }}" title="{{ match.countryA|trans }}"></td>
                <td class="team"><a href="{{ path('_showteam', { 'teamid': match.idA, 'groupid': group.id }) }}">{{ match.teamA }}</a></td>
                <td class="flag"><img src="{{ imagepath }}/flags/{{ match.flagB }}" alt="{{ match.countryB|trans }}" title="{{ match.countryB|trans }}"></td>
                <td class="team"><a href="{{ path('_showteam', { 'teamid': match.idB, 'groupid': group.id }) }}">{{ match.teamB }}</a></td>
                <td class="match">{{ match.scoreA }}-{{ match.scoreB }}</td>
            </tr>
{% endfor %}
        </tboby>
        <tfoot>
            <tr>
                <td colspan="9"></td>
            </tr>
        </tfoot>
    </table>
</p>
{% endblock %}