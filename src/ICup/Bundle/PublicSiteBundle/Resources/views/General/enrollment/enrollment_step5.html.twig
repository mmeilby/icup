{% extends "ICupPublicSiteBundle:Masters:standard.html.twig" %}

{% form_theme form 'ICupPublicSiteBundle:Default:layout.script.html.twig' %}
{% trans_default_domain "club" %}

{% block body %}
<div class="row">
    <div class="col-md-6">
        {{ sonata_block_render({'name': '/cms/content/blocks/contact/address'}) }}
    </div>
    <div class="col-md-6">
        <h2><i class="fa fa-ticket"></i> Registrering og tilmelding (del 5)</h2>
        <h3>Ankomst- og afrejse-detaljer</h3>
        <h4>Oplys tidspunkt for ankomst og afrejse</h4>
        <p>Hvis klubben selv kommer til eller fra Teramo angives her det forventede ankomst- eller afrejsetidspunkt.</p>
        <p><strong>Ankomst til Teramo</strong></p>
        <div class="row">
            <div class="col-lg-6">{{ form_row(form.arrival_date) }}</div>
            <div class="col-lg-6">{{ form_row(form.arrival_time) }}</div>
        </div>
        <p><strong>Afrejse fra Teramo</strong></p>
        <div class="row">
            <div class="col-lg-6">{{ form_row(form.departure_date) }}</div>
            <div class="col-lg-6">{{ form_row(form.departure_time) }}</div>
        </div>
        <p>Hvis klubben har behov for at blive hentet eller bragt til en lufthavn angives her det forventede fly ankomst- eller afgangstidspunkt.</p>
        <p><strong>Transport til Teramo med bus</strong></p>
        <div class="row">
            <div class="col-lg-4">{{ form_row(form.b_arrival_airport) }}</div>
            <div class="col-lg-4">{{ form_row(form.b_arrival_date) }}</div>
            <div class="col-lg-4">{{ form_row(form.b_arrival_time) }}</div>
        </div>
        <p><strong>Transport fra Teramo med bus</strong></p>
        <div class="row">
            <div class="col-lg-4">{{ form_row(form.b_departure_airport) }}</div>
            <div class="col-lg-4">{{ form_row(form.b_departure_date) }}</div>
            <div class="col-lg-4">{{ form_row(form.b_departure_time) }}</div>
        </div>
        <p>
            {{ form(form) }}
        </p>
    </div>
</div>
{% endblock %}